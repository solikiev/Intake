// Top-level build file where you can add configuration options common to all sub-projects/modules.
// Note: This is a template Android project structure.
// To build this project, you need:
// 1. Android Studio with Android SDK installed
// 2. Network access to download dependencies from Google Maven and Maven Central
// 3. JDK 17 or later

plugins {
    // These plugins require Android SDK and network access to Google Maven repository
    // Uncomment when building in Android Studio or with proper Android environment
    // id("com.android.application") version "8.1.4" apply false
    // id("org.jetbrains.kotlin.android") version "1.9.20" apply false
    // id("com.google.devtools.ksp") version "1.9.20-1.0.14" apply false
}
